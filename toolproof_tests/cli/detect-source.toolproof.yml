name: CLI detect-source subcommand

steps:
  - I run "cp -R %test_sites%/vonge-jekyll src"
  - ref: ./../core/run_gadget_cli.toolproof.yml
  - I run "npm -s run gadget -- detect-source src"
  - snapshot: stdout
    snapshot_content: |-
      ╎{
      ╎  "source": "site",
      ╎  "ssg": "jekyll"
      ╎}
